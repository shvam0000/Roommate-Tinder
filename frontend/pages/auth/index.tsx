import Hero from '@/components/auth/hero';
import { Layout } from '@/layout';
import React from 'react';

const Auth = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};

export default Auth;
